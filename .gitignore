.DS_Store
/.bundle/
/vendor/
/probe-reports/
# Holds INWORLD_API_KEY.
.env
# What the coach said, synthesised on the fly and named at random. The pre-recorded audio a workout
# ships with lives in a per-workout subdirectory and is versioned; these are throwaway.
/public/audio/inworld_*.mp3
# The raw Concept2 newsletter pages the WOD import fetches, kept so a re-run does not hit their
# server 1500 times again. What is derived from them is committed; the HTML is not.
/tmp/

# A pasted terminal scrollback, committed by an over-broad `git add -A`. Not source.
last_ride_log.txt
